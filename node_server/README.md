# Node Server

This directory contains the services that make up the node server component of the application.

## XRay Configuration

**Important:** The XRay configuration is NOT managed within this server. It is maintained by panel server.

## Shared Files

The `var` directory contains shared static web assets, such as `robots.txt`, which are served by Nginx.
